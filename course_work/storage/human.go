package storage

import (
	"github.com/goriiin/cw/structures/user"
)

func (s *Storage) humanSelect() ([]user.Human, error) {
	const op = "storage-humanSelect"
	return nil, nil
}

func (s *Storage) humanInsert(human *user.Human) error {
	const op = "storage-humanInsert"
	return nil
}
