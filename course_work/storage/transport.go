package storage

import (
	"github.com/goriiin/cw/structures/transport"
)

func (s *Storage) transportSelect() ([]transport.Transport, error) {
	const op = "storage-transportSelect"
	return nil, nil
}

func (s *Storage) transportInsert(transport *transport.Transport) error {
	const op = "storage-transportInsert"
	return nil
}
