package storage

import (
	"github.com/goriiin/cw/structures/transport"
)

func (s *Storage) damageSelect() ([]transport.Damage, error) {
	const op = "storage-damageSelect"
	return nil, nil
}

func (s *Storage) damageInsert(damage *transport.Damage) error {
	const op = "storage-damageInsert"
	return nil
}
